[English version](README.md)

# Каталог PHP-трейтов

Данный каталог предназначен для размещения PHP-трейтов (`trait`), используемых в библиотеке Eleanor PHP Library.

## Принцип работы автозагрузчика

Автозагрузчик обслуживает только пространство имён `Eleanor\Traits`. При запросе трейта автозагрузчик преобразует имя трейта в имя файла, приводя его к нижнему регистру и дополнительно проверяя kebab-case вариант имени. Например, запрос:

```php
Eleanor\Traits\TestTrait
```

приведёт к последовательной проверке файлов:

```text
testtrait.php
test-trait.php
```

Поддерживается организация трейтов через подкаталоги. Каждый подкаталог соответствует дополнительному уровню пространства имён внутри `Eleanor\Traits`. Например, для трейта:

```php
Eleanor\Traits\Test\SomeFeatureTrait
```

автозагрузчик выполнит поиск файлов:

```text
./test/somefeaturetrait.php
./test/some-feature-trait.php
```

---

## Правила и требования

### 1. Именование файлов

Автозагрузчик поддерживает оба варианта имён файлов:

```text
testtrait.php
test-trait.php
```

Имена файлов регистрозависимы и должны быть записаны в нижнем регистре.

### 2. Независимость от namespace и имени трейта

Автозагрузчик определяет соответствие трейта исключительно по имени файла. Фактическое пространство имён и имя трейта внутри файла не участвуют в поиске и могут быть произвольными. Благодаря этому каталог может использоваться как адаптерный слой для сторонних библиотек или внешних трейтов.

### 3. Возврат полного имени трейта (FQCN)

Если трейт внутри файла не соответствует ожидаемому имени трейта в `Eleanor\Traits`, файл должен вернуть полное квалифицированное имя трейта (FQCN):

```php
return \Vendor\Package\SomeTrait::class;
```

Пример:

```php
<?php
namespace Vendor\Package;

trait SomeTrait
{
    protected function helper():void
    {
        // ...
    }
}

return \Vendor\Package\SomeTrait::class;
```

После загрузки файла автозагрузчик:

- использует трейт напрямую, если он уже принадлежит пространству имён `Eleanor\Traits`;
- либо автоматически создаёт alias внутри `Eleanor\Traits`.

### 4. Автоматическое создание alias

Трейт не обязан принадлежать пространству имён `Eleanor\Traits`. Если файл возвращает FQCN трейта из другого пространства имён, автозагрузчик автоматически создаёт alias внутри `Eleanor\Traits`, обеспечивая доступ к трейту через единый механизм автозагрузки. Например:

```php
Eleanor\Traits\Test\SomeFeatureTrait
```

может фактически ссылаться на:

```php
\External\Library\FeatureTrait
```

### 5. Необязательный `return`

Если трейт уже объявлен в ожидаемом пространстве имён и его имя соответствует имени файла, файл может не возвращать FQCN. Например, файл:

```text
test/some-feature-trait.php
```

может напрямую содержать:

```php
<?php
namespace Eleanor\Traits\Test;

trait SomeFeatureTrait
{
    protected function helper():void
    {
        // ...
    }
}
```

В этом случае автозагрузчик обработает трейт автоматически без дополнительного `return`.